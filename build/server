#!/bin/bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd $DIR

if [ $# -eq 2 ]
then
    python2 ../src/main.py "$1" "$2"
elif [ $# -eq 1 ]
then
    python2 ../src/main.py "$1"
else
    exit 255
fi
